import React, { Component } from 'react';

class show extends Component {
    render() {
        return (
            <div>
                <h1>Desde mostrar!</h1>
            </div>
        );
    }
}

export default show;
